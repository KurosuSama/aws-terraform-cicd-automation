---
plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  tag:Name: ASG-WebApp-Server
  instance-state-name: running

keyed_groups:
  - key: tag.Name
    prefix: ""

compose:
  ansible_host: public_ip_address